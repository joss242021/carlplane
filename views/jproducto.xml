<odoo>
    <data>
        <!-- Campos Ocultados en la Vista "Formulario" (Form) -->
        <record id="jproducto_carlplane_view_form" model="ir.ui.view">
            <field name="name">jproducto.carlplane.view.form</field>
            <field name="model">product.template</field>
            <field name="type">form</field>
            <field name="inherit_id" ref="product.product_template_form_view"/>
            <field name="arch" type="xml">

                <field name="taxes_id" position="replace">
                    <field name="taxes_id" invisible="1"/>
                </field>
                
            </field>
        </record>
    </data>
</odoo>
